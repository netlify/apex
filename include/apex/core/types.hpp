#ifndef APEX_CORE_TYPES_HPP
#define APEX_CORE_TYPES_HPP

/* Sometimes we just want a lightweight include of types from the prelude.
 * This will rarely happen, however, unless we are writing a library meant to
 * be lightweight in terms of overhead for certain headers
 */
#include <apex/detail/prelude/types.hpp>

#endif /* APEX_CORE_TYPES_HPP */
